FROM node:20.5.1
COPY src/orabot /tmp/src
COPY tls/ /tmp/tls
WORKDIR /tmp/src
RUN npm install
RUN npm install -g serve
RUN npm run build
EXPOSE 3000
CMD ["serve", "-s", "--ssl-cert", "/tmp/tls/cert.pem", "--ssl-key", "/tmp/tls/key.pem", "build"]